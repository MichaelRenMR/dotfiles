# .bash_profile

# Get the aliases and functions
if [ -f ~/.bashrc ]; then
	. ~/.bashrc
fi

# Get the profile
if [ -f ~/.profile ]; then
	. ~/.profile
fi

# User specific environment and startup programs
